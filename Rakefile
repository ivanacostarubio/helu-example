# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project/template/ios'

require 'rubygems'
require 'helu'

Motion::Project::App.setup do |app|
  # Use `rake config' to see complete project settings.

  # REMEMBER FOR THIS TO WORK YOU MUST CONFIGURE 
  # YOUR APP IDENTIFIER WITH APPLE

  #app.name = 'YOU NAME'
  #app.identifier = "YOUR IDENTIFIER"

  app.name = 'helu-example'
  app.frameworks += ["StoreKit"]
end
